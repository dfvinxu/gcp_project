steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      - functions
      - deploy
      - ./cloud_function/cloud_function.py
      - --runtime
      - python39
      - --source=./cloud_function
      - --trigger-topic
      - cloud-builds
      - --region=europe-west1